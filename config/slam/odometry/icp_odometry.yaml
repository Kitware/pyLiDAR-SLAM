# @package _group_

algorithm: "icp_F2M"
max_num_alignments: 100

data_key: input_data # The pointcloud on which the ICP is performed

local_map: ??? # config of the local map
initialization: ??? # config of the initialization
alignment: ??? # config of the rigid alignment
preprocessing:
  filters: none # config for the preprocessing filters see odometry/preprocessing